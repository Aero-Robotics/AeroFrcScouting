const electron = require("electron");
const { ipcRenderer } = electron;
const $ = require("jquery");
const fs = require("fs");

$(document).on('ready', () => {
  $("#mySelect1", (select) => {
      
  })
})